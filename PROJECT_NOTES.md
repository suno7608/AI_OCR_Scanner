# 명함 · 영수증 스캐너 — 프로젝트 메모

> 이 문서는 새 대화에서 작업을 이어가기 위한 맥락 요약입니다.
> 함께 올린 `scanner_app.jsx`가 현재까지 완성된 앱 코드입니다.

## 한 줄 요약
사내 임원·비서가 쓰는 **private 앱**. 명함/영수증을 사진으로 올리면 Claude가
유형을 자동 판별해 정보를 추출하고, 명함은 연락처(.vcf)로, 영수증은 경비 내역으로 정리한다.

## 핵심 설계 결정 (왜 이렇게 만들었나)

- **형태**: 네이티브 앱이 아니라 **웹앱(React)**. 설치·심사·기기등록 없이 링크 하나로 공유하기 위함. 최종적으로 PWA로 홈화면 설치 예정.
- **파싱 방식**: 별도 OCR 엔진/정규식 없이 **Claude 멀티모달 1회 호출**로 유형판별 + OCR + 구조화(JSON)를 한 번에 처리. (코드의 `parseWithClaude`)
- **사용 대상**: 공용 public 앱 아님. 사내 소수 신뢰 그룹. 명함·영수증은 보안 민감 정보가 아니라는 전제.
- **데이터 저장**: Google Sheets 공유 방식은 폐기(사용자 간 데이터 섞임 문제). 대신 **앱이 사용자별로 격리 저장**(Upstash Redis, `user:{이름}` 키에 JSON 문서) + 필요 시 CSV 내보내기. (Supabase는 비용/무료한도 문제로 2026-07 Upstash Redis로 전환)

## 사용자/인증 구조

- **이름 + 4자리 PIN**으로 사용자 구분. 다른 사용자의 명함·영수증은 PIN 없이 접근 불가.
- **관리자 계정 별도**: 관리자 비밀번호로 로그인 → 각 사용자 PIN 확인 / PIN 재설정 / 사용자 삭제. (단, 데이터 내용 자체는 관리자도 못 봄)
- 관리자 기본 비밀번호: `admin1234` (코드 상단 `ADMIN_PASSWORD` 상수 — **배포 시 반드시 변경**).
- PIN 분실 시 → 관리자가 PIN 확인/재설정해주는 흐름.
- **보안 수준 주의**: PIN은 평문 저장(관리자가 평문 확인 가능). 암호학적 보안이 아니라 "서로 못 들여다보게" 막는 실용적 장벽. 사용자에게 은행 등과 다른 PIN 쓰도록 안내 권장.

## 기능 현황

### 명함
- 추출 필드 순서: 이름 → 직책 → 부서/팀 → 회사 → 휴대폰 → 유선전화 → 팩스 → 회사이메일 → 이메일2 → 회사URL → 우편번호 → 주소
- **여러 장 한 번에 업로드(멀티)** 지원 — 방식 A(큐): 전부 인식 후 "1/N"으로 한 장씩 확인·저장, 건너뛰기 가능. 진행률 표시.
- 저장 후 목록에서 **연락처에 추가(.vcf)** → iPhone 연락처 등록.

### 영수증
- 추출 필드: 날짜 / 가맹점 / 금액 / 카테고리 / 결제수단 / 비고 (+입력일 자동)
- **단건 처리만** (여러 건 불필요하다고 결정).
- 월별·카테고리별 **집계 요약**, **기간 필터**(전체/이번달/지난달), **상세보기+수정**, **중복 촬영 경고**(같은 가맹점·금액·날짜).
- **CSV 내보내기**(현재 선택된 기간 기준). 열: 입력일, 날짜, 가맹점, 금액, 카테고리, 결제수단, 비고, 입력자.

### 공통
- 입력 날짜(업로드 시점) 자동 기록.
- 한 번에 올린 사진에 명함·영수증이 섞여도 장마다 자동 판별해 분기.

## 코드 구조 메모 (`scanner_app.jsx`, 단일 React 컴포넌트)

- `App`: 인증 라우팅 (pickUser / createUser / enterPin / admin / home)
- 인증 화면: `UserPicker`, `CreateUser`, `SetPin`, `EnterPin`, `AdminLogin`, `AdminPanel`, `PinPad`, `AuthShell`
- `Home`: 탭(스캔/명함/영수증) + 사용자별 저장 키 `cards:{이름}`, `receipts:{이름}`
- `ScanView`: 멀티 업로드 큐 처리
- `ReviewCard`: 명함/영수증 확인·수정
- `CardsView` / `ReceiptsView` / `ReceiptDetail`
- 유틸: `parseWithClaude`(Claude API 호출), `downloadVCard`(iOS data URI 방식), `downloadCSV`, `fmtDate`, `parseNum`
- 저장: 아티팩트의 `window.storage` 사용 (사용자별 개인 공간)

## 알려진 제약 / 이슈

- **vCard(.vcf)와 iOS**: claude.ai 아티팩트는 iframe 샌드박스라 `window.open`/다운로드가 막혀 연락처 등록이 **아티팩트 안에서는 작동 안 함**. → Vercel 배포 시 해결됨. (코드는 iOS용 data URI 방식 + 데스크톱/안드로이드용 다운로드 폴백 적용)
- vCard는 완전 자동이 아니라 "연락처 카드 뜨면 탭 한 번"이 들어감 (웹앱의 구조적 한계).

## 다음 단계 (TODO)

1. **API 키 기반 Vercel 배포** ← 다음에 할 일
   - 현재 `parseWithClaude`는 아티팩트 환경에서 키 없이 호출됨. 독립 배포 시 **본인 Anthropic API 키 필요**.
   - 키가 브라우저에 노출되지 않도록 **Vercel 서버리스 함수(API 프록시)** 경유 구조로 변경해야 함.
   - 모델: 기본 `claude-sonnet-4-6`, 정확도 우선 시 `claude-opus-4-6`. (구버전 `claude-sonnet-4-20250514`는 폐기됨)
   - 배포 후 `ADMIN_PASSWORD` 변경, PWA 설정(홈화면 설치), Safari에서 .vcf 연락처 등록 실제 검증.
2. (선택) 영수증 → Google Sheets/Drive 연동은 "상시 저장"이 아니라 "월말 내보내기" 용도로만 고려.

## 미해결로 남겨둔 질문
- Anthropic API 키 보유 여부
- 배포 방식: 코드 패키징해서 직접 Vercel 업로드 vs 연결된 Vercel로 진행
